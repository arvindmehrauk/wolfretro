<%= form_with(model: retro) do |form| %>
  <% if retro.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retro.errors.count, "error") %> prohibited this retro from being saved:</h2>

      <ul>
        <% retro.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :context, style: "display: block" %>
    <%= form.text_area :context %>
  </div>

  <div>
    <%= form.label :team, style: "display: block" %>
    <%= form.number_field :team %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
